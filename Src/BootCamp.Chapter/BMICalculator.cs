using System;
using System.Collections.Generic;
using System.Text;

namespace BootCamp.Chapter
{
    public static class BMICalculator
    {
        public static float CalculateBMI(Person person)
        {
            if (person == null) throw new ArgumentNullException();

            return 0.0f;
        }
    }
}
