namespace BootCamp.Chapter.V1
{
    // Does all that robot does.
    // Does not move, nor talk, just assembles cars
    class CarAssemblerRobot
    {
        public void Assemble(string car)
        {
        }
    }
}